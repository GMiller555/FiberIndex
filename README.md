# FiberIndex
A database of crochet patterns

Install flask to run this program, run pip install flask in your terminal.
You also need pandas to run the program. To install, run pip install flask pandas
To run the program type python app.py in your terminal
